# 菜谱推荐系统技术路线与实验结果

> 项目：CF-HybridRec（协同过滤混合菜谱推荐）
> 本文档记录从「推荐趋同、模型发散」到「收敛 + 个性化推荐」的完整技术路线、关键问题定位与最终实验结果。

---

## 1. 问题背景

初始版本基于原始增强 SQLite（505 用户）运行时，存在三个严重问题：

1. **推荐高度趋同**：不同用户 Top-10 推荐几乎完全一样（前 3 名均为锦绣鱼皮、红枣炖兔肉、茄汁鱼条），没有个性化。
2. **用户特征匮乏**：`user` 表有效特征仅 21 维（性别 3 + 年龄 7 + 口味 11），职业、出生地、工作地全部为空，UserCF 的画像相似度无法区分用户。
3. **数据稀疏**：稠密度仅 0.72%，每个用户约 24 条有效交互，协同过滤信号极弱。

## 2. 数据增强

### 2.1 目标

生成 1000 个多样化用户，填充缺失特征，构造有明显差异的交互行为。

### 2.2 用户原型设计

`generate_new_users.py` 定义 8 种用户原型 × 口味偏移变体：

| 原型 | 性别 | 年龄 | 口味偏好 | 交互模式 |
|------|------|------|---------|---------|
| 嗜辣青年 | 男 | 18-35 | 辛、酸、咸 | 偏荤，高浏览 |
| 养生老年 | 男 | 55-80 | 甘、淡 | 清淡，高规避 |
| 精致主妇 | 女 | 25-50 | 甘、淡、酸 | 均衡，高下厨 |
| 健身青年 | 男 | 20-40 | 淡、甘 | 高蛋白 |
| 甜食女生 | 女 | 15-30 | 甘、淡 | 偏甜品 |
| 嗜辣女生 | 女 | 18-40 | 辛、酸、咸 | 偏荤 |
| 传统中老 | 女 | 45-75 | 咸、甘 | 均衡，高规避 |
| 美食探索者 | 男 | 25-55 | 什么都吃 | 浏览多，规避少 |

每种原型通过口味水平、菜系偏好、交互强度三个维度区分，生成差异化行为。

### 2.3 特征补全

- **职业**：从 `occupation` 表填充
- **出生地 / 工作地**：从 `address` 表填充
- **口味偏好**：`usertaste` 按原型分配差异化 level

### 2.4 数据指标对比

| 指标 | 原始库 | 增强库 | 提升 |
|------|--------|--------|------|
| 用户数 | 505 | 1505 | ×3 |
| 用户特征维度 d_u | 21 | 128 | ×6 |
| 交互稠密度 | 0.72% | 19.9% | ×28 |
| 总交互数 | 1.5 万 | 122 万 | ×82 |
| 喜好标记 | 0.55 万 | 62 万 | ×113 |
| 实际做菜 | 777 | 31.8 万 | ×409 |

## 3. 特征工程

### 3.1 用户特征向量 U（128 维）

| 特征组 | 维度 | 来源 | 编码 |
|--------|------|------|------|
| 性别 | 3 | `user.gender` | OneHot |
| 年龄组 | 7 | `user.birthday` → 推算 | OneHot |
| 职业 | 7 | `user.occupation` | OneHot |
| 口味偏好 | 11 | `usertaste` | level/5 归一化 |
| 出生地 | 50 | `user.birthplace` | OneHot |
| 工作地 | 50 | `user.workplace` | OneHot |

### 3.2 菜谱特征向量 V（1145 维）

| 特征组 | 维度 | 来源链路 | 编码 |
|--------|------|---------|------|
| 菜系 | 14 | `recipe.cuisine` | OneHot |
| GI/时长/成本 | 3 | `recipe` | 归一化 |
| 口味聚合 | 11 | 食材 → ingredient2taste | 分布均值 |
| 食性聚合 | 10 | 食材 → ingredient2nature | 分布均值 |
| 营养成分 | 5 | recipecomposite → composition | 归一化 |
| 食材类型 | 1076 | ingredient.foodtype | TF-IDF |
| 烹饪方式 | 26 | cookstep.cookmethod | MultiHot |

### 3.3 交互 → 伪评分映射

| 交互来源 | 信号 | 评分 |
|---------|------|------|
| `useracutalrecipe` 实际做过 | 强正 | 5.0 |
| `userfondnessrecipe` 喜好 | 正 | 3.0 + intensity×0.4 |
| `userbrowse` 浏览 | 弱正 | 2.5 |
| `useravoidrecipe` 规避 | 负 | 1.0 |

## 4. 模型架构

四模块混合推荐 + 动态置信度融合：

```text
SQLite → 特征工程(U/V) → 评分矩阵 R → 训练/测试划分(8:2)
  ├─ UserCF:   用户相似度(评分+画像自适应融合) KNN 预测
  ├─ ItemCF:   物品相似度(共现+内容自适应融合) KNN 预测
  ├─ SVD:      FunkSVD，菜谱因子用内容特征初始化
  └─ TwoTower: 双塔模型 R_hat = mu + w_u·U + w_v·V + (A^T U)·(B^T V)
  → HybridFusion: 网格搜索基础权重 × 动态置信度 phi(c_u, c_i)
  → 输出 Top-K 推荐
```

融合公式：

$$\hat{R}_{final} = w_1 \hat{R}_{UBCF} + w_2 \hat{R}_{IBCF} + w_3 \hat{R}_{SVD} + w_4 \hat{R}_{TwoTower}$$

$$w_k'(c_u, c_i) = w_k^{base} \cdot \phi_k(c_u, c_i)$$

$$\phi_1 = c_u(1-c_i) + 0.5 \quad (\text{UserCF：用户密集、物品稀疏时高})$$

$$\phi_2 = (1-c_u)c_i + 0.5 \quad (\text{ItemCF：用户稀疏、物品密集时高})$$

$$\phi_3 = c_u c_i + 0.3 \quad (\text{SVD：两者都密集时高})$$

$$\phi_4 = (1-c_u)(1-c_i) + 0.3 \quad (\text{TwoTower：两者都稀疏时兜底})$$

用户端置信度：

$$c_u = 1 - e^{-\gamma_u \cdot N_u}$$

物品端置信度：

$$c_i = 1 - e^{-\gamma_i \cdot N_i}$$

## 5. GPU 加速与调优历程

> 使用 NVIDIA RTX 3090（24GB），CuPy 14.x 作为 NumPy 的 GPU 替代。

### 5.1 GPU 化方案

将 NumPy 全部替换为 CuPy，算法逻辑不变：

| 环节 | GPU 化方式 |
|------|-----------|
| 特征工程 | CPU numpy 构建，一次性转 GPU |
| cosine_similarity | 自实现 GPU 版本（替代 sklearn） |
| TruncatedSVD | 自实现随机 SVD + 幂迭代 |
| UserCF/ItemCF | 双重循环 → 矩阵乘法向量化 |
| HybridFusion | 网格搜索逐样本 → 整批矩阵误差计算 |

### 5.2 关键问题与修复

| 问题 | 现象 | 根因 | 修复 |
|------|------|------|------|
| 逐元素 GPU 循环过慢 | UserCF 卡死 | GPU kernel 启动开销大 | 循环向量化为矩阵运算 |
| CuPy/numpy 混用 | `Implicit conversion` 报错 | numpy 随机初始化 p_u/q_i | `_to_gpu()` 统一转换 |
| SVD mini-batch 不收敛 | RMSE 卡在 1.5 | 求和梯度过大导致震荡 | 改平均梯度（除以出现次数） |
| TwoTower 梯度爆炸 | RMSE=NaN | 高维 V 的 TF-IDF 特征使梯度过大 | 平均梯度 + 误差裁剪 [-3,3] |
| SVD GPU 仍不收敛 | RMSE 卡 1.49 | CuPy mini-batch 列切片原地更新失效 | **SVD 改回 CPU 逐样本 SGD** |

### 5.3 最终部署架构

| 模型 | 运行位置 | 方式 | 理由 |
|------|---------|------|------|
| UserCF | GPU | 向量化 | 矩阵运算，GPU 收益大 |
| ItemCF | GPU | 向量化 | 矩阵运算，GPU 收益大 |
| SVD | CPU | 逐样本 SGD | GPU mini-batch 不稳定，CPU 版确定收敛 |
| TwoTower | GPU | mini-batch + 平均梯度 + 裁剪 | 已验证收敛 |
| HybridFusion | GPU | 向量化网格搜索 | 矩阵运算，GPU 收益大 |

## 6. 实验结果

### 6.1 模型收敛情况

| 模型 | 初始 RMSE | 最终 RMSE | 轮数 |
|------|:---:|:---:|:---:|
| SVD（CPU 逐样本） | 0.7666 | **0.4369** | 20 |
| TwoTower（GPU mini-batch） | 1.4044 | **1.0046** | 40 |

### 6.2 融合权重

```
UBCF=0.10  IBCF=0.10  SVD=0.10  TwoTower=0.70
```

> 说明：网格搜索在训练集上选出 TwoTower 主导。尽管 SVD 训练 RMSE 最低（0.437），但融合后测试集 RMSE 最优（0.622），说明 TwoTower 的泛化更稳。

### 6.3 离线评估（测试集 244,346 条）

| 指标 | 旧数据结果 | 增强数据结果 |
|------|:---:|:---:|
| **RMSE** | 1.24 | **0.6223** |
| **MAE** | 1.01 | **0.479** |
| Precision@20 | 0.0004 | **0.1116** |
| Recall@20 | 0.0036 | **0.2188** |
| NDCG@20 | 0.0018 | **0.1699** |
| Coverage | 1.2% | **13.4%** |

### 6.4 推荐个性化验证

修复前：所有用户 Top-10 高度趋同（前 3 名均相同）。

修复后：不同画像用户获得不同推荐：

| 用户 | 画像 | 推荐风格 | 预测分 |
|------|------|---------|:---:|
| 1001026 | 老年·女 | 粤菜系：片皮乳猪、柱侯甑肥鹅 | 4.2 |
| 1001760 | 青年·女 | 川湘鄂：蟹粉菜心、小煎兔 | 4.7 |
| 1001115 | 青年·女 | 助眠茶饮：迷迭香奶茶、三花宁神茶 | 3.4 |
| 1001105 | 青年·女 | 海鲜大菜：虾茸莲莲塔、红烧大群翅 | 4.3 |

## 7. 发现的问题与原因分析

### 7.1 低分用户现象

部分用户（1001229、1001143、1001755）Top-10 预测分仅 ~3.0。诊断发现：

| 用户 | 规避数 | 规避占比 | 平均伪评分 |
|------|:---:|:---:|:---:|
| 1001229 | 799 | 92% | 1.17 |
| 1001143 | 521 | 90% | 1.23 |
| 1001755 | 374 | 83% | 1.39 |

**根因**：数据生成时 `avoid_rate=0.2` 过于激进，给用户塞了 ~800 条 1 分规避记录，把评分基线压到 1.2~1.4。

**模型行为合理性**：模型正确学到了"该用户给分低"，但其 Top-10 预测（~3.0）仍高于训练平均（1.2），说明模型认为推荐项优于用户平均偏好。

**修复建议**：将 `generate_new_users.py` 的 `avoid_rate` 从 0.2 降至 0.03 左右，重新生成数据。

### 7.2 TwoTower 主导融合的原因

1. **SVD 在 GPU mini-batch 下不收敛**（已改 CPU 修复），此前权重被不公平压低。
2. **TwoTower 是唯一同时利用用户特征 U 与物品特征 V 的参数化交互模型**，能学习「哪类用户 × 哪类菜」的交叉偏好，正好吃到了新增用户多样性的红利。
3. **UserCF/ItemCF 是内存型方法**（加权平均邻居/相似物品），表达力有天然上限。

## 8. 后续改进方向

- 修复数据生成规避率，生成更均衡的用户
- 引入 BPR (Bayesian Personalized Ranking) 直接优化排序
- MMR 多样性重排缓解 Popularity Bias
- 规则层过滤接入健康/季节/运动推荐表
- 使用 PyTorch 重写训练部分，真正利用 GPU 加速 SVD

## 9. 运行方式

```bash
# 生成增强数据库（可选，已生成可跳过）
python generate_new_users.py

# CPU 版
python cf_demo_v2.py

# GPU 加速版（需要 CuPy + NVIDIA GPU）
python cf_demo_v2_gpu.py

# 快速版（跳过 ItemCF，CPU 3-5 分钟）
python cf_demo_v2_fast.py
```

依赖：`pip install numpy pandas scikit-learn cupy-cuda11x`
